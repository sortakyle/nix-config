# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"
tasks:
  iso:
    desc: Build ISO Image [REBUILD=false]
    cmds:
      - nix build .#nixosConfigurations.iso.config.system.build.isoImage {{ if eq "true" .REBUILD }}--rebuild{{ end }}
